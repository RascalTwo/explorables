# Development docs — design context + ADRs. Kept locally for whoever maintains
# this skill, but intentionally NOT committed (they're not needed to *run* the
# skill; SKILL.md is the execution-time manual). grill-with-docs writes
# CONTEXT.md at the skill root and ADRs under docs/.
CONTEXT.md
docs/

# Publish/preview build output — generated by build.ts, never source.
.viz-dist/

# verify.ts render output — regenerated each run, never source.
.verify/
node_modules/
