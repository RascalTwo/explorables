.DS_Store
node_modules/
comments.json
og.auto.png
.verify/
.viz-dist/
CLAUDE.md

# viz mirror config: sibling-repo paths — local-only, never commit
viz-pages/mirrors.json

# (The viz self-portrait used to be ignored here as a generated mirror. It never
# was one — the bundle's mirrors.json only ever declared ai-setup/viz-pages as a
# sink, so nothing ever projected a copy in and the rule guarded a directory that
# did not exist. The self-portrait lives solely in the skill bundle now.)

# Generated by build.ts on each publish (the viz index page + its hero thumbnails)
viz-pages/index.html
viz-pages/_thumbs/

# viz mirror config: sibling-repo paths — local-only, never commit
skills/viz/viz-pages/mirrors.json
