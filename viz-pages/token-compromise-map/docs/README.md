# token-compromise-map — design record

**The design record for this page lives with its siblings**, in
[`../../jwks-explainer/docs/`](../../jwks-explainer/docs/) — one record covers all
four pages, because they are one deliverable and splitting it would guarantee drift.

The decisions specific to this page are **D9** (why the four InfoSec questions are
two pages, and where the generic/deployment-specific line sits) and **D10** (the
two-universe treatment of the signing key) in
[`01-decisions.md`](../../jwks-explainer/docs/01-decisions.md).
